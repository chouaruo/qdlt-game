# 问道链途 · Claude Code 工作手册

你正在接手一个由 Claude Chat 开发了相当长时间的项目。
请在开始任何任务前完整读完这份文档。

---

## 项目概述

**问道链途** 是一款币圈题材的中文文字冒险游戏。
世界观核心隐喻：**修真 = 炒币**，境界 = 资产等级，灵力 = 本金，宗门 = 交易所。

当前主文件：`index.html`（单文件，含全部 CSS + HTML + JS）
白卡UI版本：`index-wechat.html`（微信小程序风格，同逻辑，换了视觉）

---

## 技术栈

- 纯前端单 HTML 文件，无构建工具
- Anthropic API（后端代理，不直接暴露 Key）
- 模型：`claude-haiku-4-5-20251001`（速度优先）
- 预生成缓存架构：AI 在后台并行生成下一节所有分支，点击时瞬间响应

### 预生成核心逻辑（不要破坏这个）
```javascript
// 玩家看当前剧情时，后台并行预生成3个分支
function prefetchAll(choices, histSnap) {
  cache = {};
  choices.forEach(async (choice) => {
    cache[choice] = await callAI('我的选择：' + choice, histSnap);
  });
}

// 点击时优先从缓存取，命中则瞬间显示，miss 才实时生成
const cached = cache[choice];
if (cached && cached.story) {
  await typeText(cached.story); // 瞬间
} else {
  const result = await callAI(...); // 等待 fallback
}
```

---

## 游戏系统设计

### 四大属性
| 属性 | 含义 | 归零后果 |
|------|------|---------|
| 本金 | 资产规模 | 破产清算，游戏结束 |
| 心态 | 抗跌能力 | 恐慌割肉，强制结局 |
| 认知 | 看穿骗局 | 永远被割，无法识别风险 |
| 气运 | 奇遇触发率 | 永远接最后一棒 |

- 开局基础值均为 0
- 玩家有 N 点（初始20，每局通关+10）可分配
- 每属性上限 +10，允许同属性词条叠加

### 境界系统（A0-A15）
```
A0  负债      凡人
A1  ¥1        练气初期
A2  ¥10       练气中期
A3  ¥100      练气后期
A4  ¥1,000    筑基初期
A5  ¥1万      筑基中期
A6  ¥10万     筑基后期
A7  ¥100万    金丹初期
A8  ¥1,000万  金丹中期
A9  ¥1亿      金丹后期
A10 ¥10亿     元婴初期
A11 ¥100亿    元婴中期
A12 ¥1,000亿  元婴后期
A13 ¥1万亿    化神初期
A14 ¥10万亿   化神中期
A15 ¥100万亿  化神后期
隐藏           渡劫（SEC起诉/交易所崩盘）
隐藏           飞升（V神/中本聪专属结局）
```

### 词条系统
- 开局随机抽 8 张，玩家选 3 张
- 保底：必含 1紫（稀有）+ 1橙（传说）
- 有上世记录时，必出上世人物专属轮回词条

词条等级与颜色：
```
🟢 绿色 普通   权重44%
🔵 蓝色 精良   权重30%
🟣 紫色 稀有   权重16%
🔴 红色 史诗   权重8%
🟠 橙色 传说   权重2%
```

词条库（20张基础，每属性各5张，绿蓝紫红橙各一）：
```
本金：工资定投🟢 / 空投猎人🔵 / 杠杆战神🟣 / 梭哈天才🔴 / VC估值魔法🟠
心态：套牢不卖🟢 / 钻石手🔵 / 熊市建仓派🟣 / 永不止损🔴 / 禅定🟠
认知：白皮书读完🟢 / 四年周期论🔵 / 链上侦探🟣 / 宏观研究员🔴 / 中本聪遗稿🟠
气运：第一批用户🟢 / 土狗猎手🔵 / 做市商朋友🟣 / 天选散户🔴 / 中本聪遗产🟠
```

### 隐藏人物（需累计点数+属性双重达标）
```
散户李狗蛋    0点     默认解锁    @无
赵长鹏 CZ    35点    本金≥60    @cz_binance
SBF          40点    认知≥70    @SBF_FTX
宝二爷       40点    心态≥70    @baoerrye
徐明星       45点    气运≥65    @StarXMX
孙宇晨       50点    气运≥75    @justinsuntron
Do Kwon      55点    认知≥85    @stablekwon
Cathie Wood  55点    认知≥80    @CathieDWood
ZachXBT      60点    认知满值   @zachxbt
Brian Armstrong 60点 本金≥70   @brian_armstrong
Arthur Hayes 60点    心态≥80    @CryptoHayes
Michael Saylor 65点  认知≥75   @saylor
何一         65点    气运≥70    @heyibinance
吴忌寒       65点    本金≥75    @JihanWu
V神 Vitalik  80点    认知≥90    @VitalikButerin
中本聪       100点   全属性≥80  无（emoji ∞）
```

头像服务：`https://unavatar.io/twitter/{用户名}`
加载失败自动降级为 emoji。

### 轮回系统
- 通关/死亡后 totalPts +10，存入 localStorage
- 上世人物记录传递下世，下世词条池必出该人物轮回词条
- AI 开篇旁白提及上世记忆

### AI System Prompt 结构
返回严格 JSON：
```json
{
  "story": "剧情100字内，币圈黑话+真实梗",
  "choices": ["选项1(12字内)", "选项2", "选项3"],
  "effects": {
    "money": -50到100,
    "mind": -20到20,
    "know": -20到20,
    "luck": -15到15,
    "item": null或字符串,
    "lose_item": null或字符串,
    "breakthrough": false或true
  }
}
```

---

## UI 设计规范（当前采用微信白卡风格）

### 配色
```css
--bg: #f5f5f5          /* 页面底色 */
--card: #ffffff         /* 卡片白 */
--border: #e8e8e8       /* 细线 0.5px */
--text1: #111111        /* 主文字 */
--text2: #555555        /* 次要文字 */
--text3: #999999        /* 辅助文字 */
--gold: #d4881a         /* 品牌金 */
--green: #07c160        /* 微信绿（成功/稳） */
--red: #fa5151          /* 危险/险 */
```

### 设计原则
- 所有卡片白底，`0.5px` 细线分隔，圆角 `8-12px`
- 按钮实心金色，无渐变
- 分组列表用 `1px` gap + 白卡，类似微信设置页
- 奇遇弹窗：移动端底部滑出，PC 端居中弹出
- Header 白底吸顶

---

## 持久化存储
```javascript
localStorage keys:
  'qdlt_save'  // JSON: { totalPts, prevChar, prevTraits, prevKey }
  'qdlt_run'   // JSON: 当前进行中的游戏状态（存档）
```

---

## 已知待完成事项（优先级排序）

### P0 - 最紧急
- [ ] **Vercel 部署**：前端静态托管 + Serverless Function 代理 API Key
  - `api/chat.js` 接收前端请求，注入 `ANTHROPIC_API_KEY` 环境变量转发
  - 前端 `callAI()` 改为请求 `/api/chat` 而非直接调 Anthropic

### P1 - 重要
- [ ] **奇遇事件库扩充**：当前约15条，目标50+条
  - 需要各人物专属奇遇（CZ发推、SBF话术、孙宇晨画饼等）
  - 高境界专属事件（元婴/化神级别的宏大事件）
  - 词条联动事件（持有「链上侦探」词条时触发特殊事件）
- [ ] **境界突破强化**：突破时全屏动画+专属奇遇触发
- [ ] **人物属性解锁验证**：当前只验证点数，需要在选角后验证属性是否达标

### P2 - 优化
- [ ] **头像加载优化**：unavatar.io 偶尔慢，加 loading skeleton
- [ ] **词条持续效果**：「杠杆战神」本金×2 目前只开局生效，应有持续机制
- [ ] **移动端键盘适配**：输入框弹出时页面偏移问题
- [ ] **分享功能**：生成游戏截图/结局分享卡片

### P3 - 长期
- [ ] **音效系统**：关键事件音效（突破、暴雷、奇遇）
- [ ] **更多人物**：继续扩充币圈知名人物
- [ ] **多语言**：英文版（面向海外币圈用户）

---

## 开发注意事项

1. **不要破坏预生成逻辑**，这是核心体验，任何修改需保证 `cache` 机制完整
2. **JSON 解析要健壮**，AI 返回格式偶尔不稳定，需要 try/catch + fallback
3. **保持单文件结构**，除非 Vercel 部署需要拆分
4. **移动端优先**，所有新增 UI 先在 375px 宽度验证
5. **词条颜色系统不要改动**，green/blue/purple/red/orange 五色有严格语义

---

## 给 Claude Code 的第一步建议

```bash
# 1. 读当前文件结构
ls -la

# 2. 确认文件完整性
grep -c "function" index.html

# 3. 开始任务前先告诉用户你理解了什么，再动手
```

如果用户说「继续开发」但没有指定具体任务，
请主动询问是从 P0（Vercel部署）开始，还是 P1（扩充事件库），还是其他。

